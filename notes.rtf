 st.write(df['Ad Group'].notna().sum())
        existing_rows = df['Ad Group'].notna().sum()
        
        start_index = existing_rows
        st.write(existing_rows)

        for i in range(3):
            df.loc[start_index + i, 'Ad Group'] = selected_adgroup

        
        Adgroupstore.append(selected_adgroup)

        st.write(df)
        